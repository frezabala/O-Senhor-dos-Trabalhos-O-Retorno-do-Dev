// Quando o usuário clicar no botão "Iniciar o Jogo"
document.getElementById('btnStart').addEventListener('click', function() {
    // Redireciona para a página do jogo
    window.location.href = 'game.html';
});

// Quando o usuário clicar no botão "Ranking"
document.getElementById('ranking').addEventListener('click', function() {
    // Redireciona para a página de ranking
    window.location.href = 'ranking.html';
});

// Quando o usuário clicar no botão "Conquistas"
document.getElementById('conquistas').addEventListener('click', function() {
    // Redireciona para a página de conquistas
    window.location.href = 'conquistas.html';
});